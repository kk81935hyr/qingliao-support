# 轻聊移动端界面设计

## 产品定位

“轻聊”是一款面向 Android 手机的轻量聊天工具，重点是让用户快速进入最近会话、阅读消息并发送回复。第一版采用本地演示数据与本地状态管理，不引入账号体系、云端同步或第三方服务。

## 屏幕列表

| 屏幕 | 核心内容 | 主要操作 |
|---|---|---|
| 会话首页 | 顶部品牌标题、搜索入口、置顶会话、最近会话列表、未读标记 | 搜索会话、打开聊天、创建新会话 |
| 聊天详情 | 联系人头像与在线状态、消息气泡、日期分隔、底部输入栏 | 返回、发送文字、添加附件入口、语音入口 |
| 新建聊天 | 联系人搜索、推荐联系人、最近联系人 | 选择联系人并进入聊天 |
| 个人资料 | 用户头像、昵称、个性签名、在线状态 | 编辑资料、进入设置 |
| 设置 | 通知、主题、隐私等基础设置项 | 切换选项、返回 |

## 主要布局

应用采用 Android 竖屏与单手操作优先的布局。首页使用顶部安全区内的品牌标题和右侧圆形“新建”按钮，搜索框位于标题下方，主列表使用 `FlatList`，每行包含 48px 圆形头像、联系人名称、最近消息、时间和可选未读徽标。聊天详情使用浅色背景，顶部导航栏高度约 56px，消息列表从下向上阅读，发送者消息靠右并使用品牌紫色气泡，接收者消息靠左使用白色或浅灰色气泡。底部输入栏固定在安全区上方，包含附件、文本输入框和发送按钮，键盘出现时输入栏保持可用。

## 关键用户流程

用户打开应用后首先看到会话首页。用户点击某一会话后进入聊天详情，可以点击输入框输入文字，点击发送按钮或键盘完成操作，消息立即追加到列表底部并显示发送反馈。用户点击首页右上角的新建按钮进入新建聊天，选择联系人后直接进入对应聊天详情。用户在聊天页点击左上角返回按钮回到会话首页，首页会更新该会话的最近消息与时间。

## 视觉与颜色

品牌使用“夜紫 + 薄荷绿”的清爽组合，体现轻量、可信和有温度的沟通体验。主色为 `#6D5DF5`，用于发送气泡、主按钮和选中状态；辅助色为 `#42D6B5`，用于在线状态、未读徽标和轻提示；背景色为 `#F6F7FB`，卡片和接收消息使用 `#FFFFFF`；主要文字为 `#171827`，次要文字为 `#85879A`，分割线为 `#E7E8F0`。暗色模式可将背景切换为 `#12131A`，但第一版优先保证浅色主题的完整体验。

## 交互细节

所有可点击元素使用明确的按压反馈；主要发送按钮使用轻微缩放和触觉反馈。输入为空时发送按钮处于弱化状态，输入有内容时切换为主色。消息列表始终使用 `FlatList`，并在新增消息后滚动到底部。界面不使用复杂动画，优先保证 Android 中低端设备上的流畅度与可读性。
